let numbers = [50, 10, 40, 30, 20];
let sorted = numbers.sort((a, b) => a - b);//quickest way to shorten the code. We do not even need to use standard function syntax this way.
console.log(sorted); // [10, 20, 30, 40, 50]
